<nav class="navbar navbar-inverse primary">
  <div class="container-fluid">
    <ul class="nav navbar-nav d-flex flex-row">
      <li><a href="/">Home</a></li>
      {% if username %}
      <li><a href="/tasks">My Tasks</a></li>
      {% endif %}
    </ul>
    <div class="nav navbar-nav navbar-right d-flex flex-row">

      <ul class="nav navbar-nav navbar-right d-flex flex-row">
        {% if username == undefined %}
        <li><a href="/signup"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
        <li><a href="/signin"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
        {% endif %}
      </ul>
      {% if username %}
          <a class="nav-link profile" href="/profile">Profile{% if username %} ({{ username }}){% endif %}</a>
          <form class="d-flex" action="/logout" method="post">
          <button class="btn btn-primary ml-10" type="submit">Logout</button>
          </form>
        {% endif %}
    </div>
  </div>
</nav>